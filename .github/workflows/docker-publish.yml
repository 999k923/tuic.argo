​:codex-file-citation[codex-file-citation]{line_range_start=1 line_range_end=75 path=README_docker.md git_url="https://github.com/999k923/tuic.argo/blob/main/README_docker.md#L1-L75"}​

---

## .github/workflows/docker-publish.yml（完整）
```yaml
name: Docker Publish

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/tuic-argo:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/tuic-argo:sha-${{ github.sha }}
