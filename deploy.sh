#!/bin/sh

# ==============================================================================
# All-in-One TUIC & VLESS/VMess+Argo 管理脚本 (v2.5 - 安装后自动显示节点)
# ... (其他部分不变) ...
# ==============================================================================

# --- 颜色定义 ---
# ... (不变) ...

# --- 脚本常量 ---
# ... (不变) ...

# --- 辅助函数 ---
# ... (不变) ...

# --- 核心功能函数 ---

do_install() {
    # ... (前面的所有安装逻辑都完全不变) ...

    cat > "$CONFIG_PATH" <<EOF
{
    "log": {"level": "info", "timestamp": true},
    "inbounds": [ ${inbounds} ],
    "outbounds": [{"type": "direct", "tag": "direct"}]
}
EOF
    print_msg "配置文件创建成功。" "green"
    
    # 启动服务
    do_start

    # 【核心优化】在安装和启动完成后，自动调用 do_list 显示节点信息
    print_msg "\n--- 安装完成，正在获取节点信息 ---" "blue"
    do_list
}

# ... (其他所有函数 do_list, do_start 等都完全不变) ...
